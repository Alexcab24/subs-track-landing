---
const navigation = [
    { name: "Dashboard", href: "/app", icon: "🏠" },
    { name: "Payment History", href: "/app/history", icon: "📅" },
    { name: "Settings", href: "/app/settings", icon: "⚙️" },
];

const currentPath = Astro.url.pathname;
---

<aside class="fixed inset-y-0 left-0 w-64 bg-white border-r border-gray-200">
    <div class="flex flex-col h-full">
        <div class="flex items-center h-16 px-6 border-b border-gray-200">
            <a href="/" class="flex gap-x-4 text-xl font-bold text-gray-900">
                <!-- SubsTrack -->

                <img src="/logotype.png" alt="" class="w-24 h-auto" />

               
            </a>
            <span class="text-xs font-normal text-yellow-600 ml-1"
            >BETA</span
        >
        </div>

        <nav class="flex-1 px-4 py-6 space-y-1">
            {
                navigation.map((item) => (
                    <a
                        href={item.href}
                        class:list={[
                            "flex items-center px-4 py-2 text-sm font-medium rounded-lg",
                            currentPath === item.href
                                ? "bg-yellow-50 text-yellow-700"
                                : "text-gray-600 hover:bg-gray-50",
                        ]}
                    >
                        <span class="mr-3">{item.icon}</span>
                        {item.name}
                    </a>
                ))
            }
        </nav>

        <div class="p-4 border-t border-gray-200">
            <div class="flex items-center">
                <div class="w-8 h-8 bg-gray-200 rounded-full animate-pulse">
                </div>
                <div class="ml-3">
                    <div class="h-4 w-24 bg-gray-200 rounded animate-pulse">
                    </div>
                    <div
                        class="h-3 w-16 bg-gray-100 rounded mt-1 animate-pulse"
                    >
                    </div>
                </div>
            </div>
        </div>
    </div>
</aside>
